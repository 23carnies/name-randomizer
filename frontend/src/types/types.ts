export interface Teammate {
  id: number;
  firstName: string;
  lastInitial: string;
  position: string;
  isAbsent: boolean;
}

// Define the interface for a TeamMember object
export interface TeamMemberData {
  id: number;
  firstName: string;
  lastInitial: string;
  position: string;
  isAbsent: boolean;
}
